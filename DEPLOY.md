Deployment steps:
1. Push this folder to GitHub and import to Vercel OR upload files to Vercel project.
2. Set environment variable OPENAI_API_KEY in project Settings > Environment Variables.
3. Deploy. The endpoint will be /api/chat
